export class Bracket {

    constructor(
        id,
        type,
        position,
        rotation,
        memberId
    ) {

        this.id = id;

        this.type = type;

        this.position = position;

        this.rotation = rotation;

        this.memberId = memberId;

    }

}